#standardsql
SELECT
  shipname,
  timestamp as timestamp,
  lat,
  lon,
  speed,
  course,
  callsign,
  fleet,
FROM
  `{{ source }}`
WHERE
  DATE(timestamp) BETWEEN '{{ start_date }}' and '{{ end_date }}'
GROUP BY 1,2,3,4,5,6,7,8
